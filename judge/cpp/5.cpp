// Now only C++ is supported

#include <bits/stdc++.h>
using namespace std;

int main() {
    int t = 10 / 0;
    // std::this_thread::sleep_for(std::chrono::milliseconds(3000));
    // int a, b;
    // cin >> a >> b;
    // cout << a + b;
}
      